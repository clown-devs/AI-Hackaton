version: '3.9'

services:
  fastapi:
    restart: "no"
    hostname: api
    container_name: api
    build:
      dockerfile: Dockerfile
    command: sh -c "uvicorn main:app --reload --host 0.0.0.0 --port 85"
    volumes:
      - ./:/AI-Hackaton
    ports:
      - '85:85'