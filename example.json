//input -> файл в формате .REC 


    
//output -> отрисовать этот json
{
    word_url: string(url),
    [
        {
            "episode": int,
            "start" : int(seconds),
            "end": int(seconds),
            "duration": int(seconds),
            "type": string,
        }
    ]
}
