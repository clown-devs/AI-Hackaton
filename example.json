//input -> файл в формате .REC 


    
//output -> отрисовать этот json
{
    "word_url": string(url),
    "dead": bool,
    "items":[
        {
            "episode": int,
            "start" : int(seconds),
            "end": int(seconds),
            "duration": int(seconds),
            "type": string,
        }
    ]
}
